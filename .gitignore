# Dependencies
/node_modules
CLAUDE.md

# Lockfiles for other package managers
bun.lock
yarn.lock

.DS_Store

ha-config/*
!ha-config/configuration.yaml
!ha-config/ui-lovelace.yaml

# Build output (built by CI and attached to the GitHub release)
dist/

# Playwright
test-results/
playwright-report/

# Local machine compose override (corporate CA, local paths)
docker-compose.override.yml
