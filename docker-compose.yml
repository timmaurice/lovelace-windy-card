name: windy-card-test
services:
  homeassistant:
    container_name: ha-windy-card-test
    image: homeassistant/home-assistant:stable
    volumes:
      - ./ha-config:/config
      - ./dist:/config/www
    ports:
      - 8123:8123
