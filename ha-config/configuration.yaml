default_config:

frontend:

# Drives the conditional-card swap in the "Autoplay Sync (#43)" view.
input_boolean:
  windy_play:
    name: Windy Play
    icon: mdi:play-circle

lovelace:
  resource_mode: yaml
  dashboards:
    lovelace:
      mode: yaml
      filename: ui-lovelace.yaml
      title: Windy
      icon: mdi:weather-windy
      show_in_sidebar: true

  resources:
    - url: /local/windy-card.js
      type: module
